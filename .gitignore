# Build artifacts
/bin/
/dist/
/spotifytool
/musictool
*.test
*.out

# Local state (never commit secrets or DB)
*.db
*.db-wal
*.db-shm
token.json
.env
.env.local

# Editor / OS
.DS_Store
*.swp

# SSH key dropped for push (never commit)
/github
id_ed25519*
*.pem

# App signing keystores (purged from history 2026-08-14; keep local-only —
# a spare copy lives at ~/.local/share/spotifytool-secrets/)
*.keystore

# session/debug droppings
*.err
build*.json

# Android build toolchain (local only)
/tools/
/android-sdk/
/.gradle/
/spotifytool.apk
/musictool.apk
